# Week 2 - API Fundamentals & Webhooks

## 📘 Overview
This project focuses on understanding the basics of APIs and Webhooks using **Express.js**.  
build a simple API, create webhook endpoints to handle POST requests, verify webhook signatures for security, and log received data into Google Sheets using the **Google Sheets API**.

---

## 🧱 Assignment Tasks

### 1️⃣ Build a Simple Express.js API That Returns JSON
Create a simple Express.js API that returns a JSON response.

### 2️⃣ Create a Webhook Endpoint to Receive POST Data
Set up a webhook endpoint that receives and logs POST data.

### 3️⃣ Create a Webhook Endpoint with Signature Verification
Implement a webhook endpoint that receives POST data and verifies the signature for security.

### 4️⃣ Log Received Data to Google Sheets
Log all received webhook data to a Google Sheet using the **Google Sheets API**.
